# PLATOON_HARD SCOUT — NEUTRALITY LANE (1 job, launched with FORCE=1).
# Owner-decided 2026-08-18: harder-OpFor follow-up as a NEW additive scenario.
#
# This job is the NEUTRALITY RE-DERIVATION: platoon at seed 12 on the tree
# that registered the platoon_hard trio (3c72cd1) and the cohesion
# measurement (450c8df). The pre-flight will correctly flag it as a duplicate
# of platoon_v8 / platoon_v10_seed12 — that is the measurement: this lane
# launches with FORCE=1 precisely so the reproduction table can say whether
# the registration moved anything. If platoon_v12_seed12 does NOT reproduce
# platoon_v8 bit-for-bit when it lands, the additive claim failed — diagnose
# before reading the platoon_hard arms as comparable to the depth cycle.
#
# FORCE is queue-wide in train_queue.sh, so this lane carries exactly the one
# job that needs it; the three platoon_hard jobs run as a separate, normal
# (pre-flighted) queue: scripts/campaigns/platoon_hard_scout.jobs.
platoon_v12_seed12   --scenario platoon   --total-steps 3000000 --seed 12
